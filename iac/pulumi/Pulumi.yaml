name: tabiya-classifier
runtime: python
description: Tabiya Livelihoods Classifier — GCP infrastructure (Pulumi Python)

# Stack-specific config (project, region, image URIs, etc.) is NOT committed here.
# It is fetched from GCP Secret Manager at deploy time by scripts/prepare.py,
# which writes a Pulumi.{stack}.yaml file that is git-ignored.
#
# Secret values (MONGODB_URI, HF_TOKEN) are loaded from the .env.{stack} file
# written by prepare.py and available as environment variables when pulumi up runs.
#
# Required config keys — see iac/templates/stack_config.template.yaml
# Required env var keys — see iac/templates/env.template
